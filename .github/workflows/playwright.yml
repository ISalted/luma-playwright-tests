name: Playwright Tests
on:
    push:
        branches: [ main ]
jobs:
    playwrights:
        runs-on: ubuntu-latest
        steps:
            - name: Checout repository
              uses: actions/checkout@v2
            - name: Run tests
              run: |
                echo "Start our tests"
                echo "Install dependencies from lockfile"
                echo "Run playwright tests"
                npm run test:ci
